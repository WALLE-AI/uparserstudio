# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository purpose

`uparserstudio` is currently a research/reference workspace, not a standalone application. The top level of this repo has no source code of its own — it exists to hold a curated set of open-source document-parsing / OCR projects side by side under `opensource/` for study, comparison, and (eventually) extraction of ideas or code into a unified parser.

Each subdirectory of `opensource/` is a **full clone of an independent upstream repository**, complete with its own `.git`, license, dependency lockfiles, and build tooling. They are not git submodules and are not otherwise wired into this repo — `git status` at the top level just shows `opensource/` as a single untracked directory. Do not assume changes inside them are tracked by the top-level repo's history unless you check `git -C opensource/<project> status` directly.

## Vendored projects

| Directory | Upstream | Language / stack | What it is |
|---|---|---|---|
| `opensource/chandra` | `datalab-to/chandra` | Python (`uv`, `pytest`) | Datalab's document-intelligence model/toolkit. |
| `opensource/dots.ocr` | `studio-dots-ai/dots.ocr` | Python | Vision-language OCR model (`dots_ocr/`) with demo and Docker deployment. |
| `opensource/liteparse` | `run-llama/liteparse` | Rust core (`crates/`) with Node.js (napi-rs), Python (PyO3), and WASM bindings (`packages/`) | Fast local-first PDF/document parser with spatial (bounding-box) text extraction and pluggable OCR. Has its own `AGENTS.md` with detailed architecture notes — read that file before making changes inside this directory. |
| `opensource/MinerU` | `opendatalab/MinerU` | Python (`mineru/`) | PDF-to-structured-data extraction pipeline (layout analysis, formula/table recognition). |
| `opensource/MonkeyOCR` | `Yuliang-Liu/MonkeyOCR` | Python (`magic_pdf/`) | Document parsing via a Structure-Recognition-Relation triplet model paradigm; includes a FastAPI service (`api/`) and `parse.py` CLI-style entry point. |

Because each project is self-contained, **work inside one of these directories using that project's own conventions and tooling** — do not assume a shared build system, dependency manager, or test runner across them. Check for that project's own `README.md`, `AGENTS.md`, `CONTRIBUTING.md`, or lockfile (`uv.lock`, `Cargo.lock`, `pyproject.toml`, etc.) to determine how to build/lint/test it. Notably `opensource/liteparse` ships an `AGENTS.md` with a full architecture writeup (Rust core → language bindings, spatial grid projection pipeline, OCR engine trait) that should be treated as authoritative when working in that project.

## Unified CLI product design

`ARCHITECTURE.md` documents the target design for `uparserstudio` as a unified CLI product (`uparser`) intended primarily for coding agents (Claude Code, Codex, OpenCode) to invoke as a subprocess. As of v0.6, **the core is implemented in Rust** (`uparser-core` crate, modeled directly on `opensource/liteparse`'s "Rust core + napi-rs/PyO3/wasm-bindgen bindings" pattern — Python/TS are binding-layer only, not implementation languages). As of v0.7, `uparser_core` supports six pluggable parsing protocols via a **Protocol Adapter** trait: four single-model VLM protocols (MinerU's `backend/vlm/`, dots.ocr, **MonkeyOCRv2** — target v2 in `opensource/MonkeyOCRv2`, not the v1 SRR-triplet code in `opensource/MonkeyOCR` — PaddleOCR), plus two new additions: **`native`** (zero-model, embeds the `liteparse` crate directly as a library dependency for born-digital PDFs with a native text layer — no external service at all) and **`pipeline`** (traditional multi-model layout→OCR→formula→table flow aligned with MinerU's `pipeline` backend). The `pipeline` protocol solves a specific problem the user raised: several of its models are heavy enough to saturate a client-side agent machine's CPU, so each of its four stages (layout/ocr/formula/table) independently declares a `StageBackend` — `Local` (in-process lightweight ONNX inference via the `ort` crate, only for genuinely small models like MinerU's already-ONNX table classifier/structure recognizer) or `Remote` (offload to a new second class of externalized service, a custom lightweight REST contract — not OpenAI chat-completions — dubbed "Pipeline Model Serving"). Defaults lean `Local` only for the lightweight `table` stage; `layout`/`ocr`/`formula` default to `Remote` since their original weights are torch models with uncertain ONNX portability (open question, see `ARCHITECTURE.md` §8/§11.2). Model inference is otherwise always externalized — never run in-process — via whatever service each protocol natively requires (VLM protocols through vLLM/LMDeploy OpenAI-compatible endpoints; PaddleOCR and Pipeline Model Serving through their own lightweight, non-chat-completions REST contracts), with one known exception under discussion: MonkeyOCRv2's document-dewarping preprocessor is a local-only auxiliary model with no externalized service equivalent yet (see `ARCHITECTURE.md` §8-§9 — this is part of why the "no in-process inference by default" line is held firmly, since Rust's ML ecosystem is weaker than Python's). Recommended engineering practice: prototype new protocol adapters in Python first (faster iteration while reverse-engineering a model's raw prompt/output contract from its vendored source), then port stabilized adapters to Rust (`ARCHITECTURE.md` §9.4).

As of v0.8, the design also covers: (1) **multi-format ingestion** — `ingest.rs` gains a `normalize_format()` step converting PPT/Word/Excel/images to PDF via external tools (LibreOffice/ImageMagick), reusing the approach `opensource/liteparse` already validated, shared by all six protocols; XLSX/CSV get a structured-data bypass (read cells directly, skip visual recognition entirely) — see `ARCHITECTURE.md` §12; (2) **content pre-analysis and routing** — a Profiler+Router stage runs before protocol selection, classifying documents in cost-increasing tiers (L1 format/metadata → L2 cheap structural heuristics → L3 *opt-in* low-cost model-based semantic classification) and producing a `DocumentProfile` written into `ParseResult`; `uparser classify` exposes it standalone and `--protocol auto` wires classify+route+parse — see §13.

v0.9 is an architecture-review revision that fixed several internal contradictions accumulated across v0.3–v0.8; when working on the design treat v0.9 notes as authoritative over older text: (a) the `ProtocolAdapter` trait was rebuilt around a single `async fn parse_page(page, ctx)` (with `#[async_trait]` for `dyn` safety) because the old `build_requests→dispatch→parse` linear trio **could not express two-stage protocols** (MinerU-vlm / MonkeyOCRv2, where stage-2 requests depend on stage-1's parsed layout) — adapters now drive their own multi-round orchestration internally via `ParseCtx` (§2.0); (b) a `scheduler.rs` was added for document-level scheduling, a shared cross-page concurrency budget, processing windows and streaming output for large docs (§2.2 — previously absent though both reference impls treat it as essential); (c) the IR gained `spans`/`merge_hint`/`font_size`, a `Geometry` enum (rect vs polygon) split out from `CoordinateSystem`, and a `geom_frame` for nested crop coordinate frames — because the flat v0.8 `Block` silently dropped signals MinerU's paragraph-merge/title-leveling depend on; shared `postprocess.rs` is now explicitly two-layer (pure-geometry vs signal-enhanced, degrading by each adapter's declared `emitted_signals`) and title-leveling is downgraded to an optional capability-gated LLM step, not a solved shared feature (§4.1/§5/§14); (d) the Agent-first CLI contract (semantic exit codes, stdout=result/stderr=logs, structured error object) was restored as first-class (§6.1) after eroding in v0.4–v0.8; (e) a content-hash cache layer was restored (§15); (f) the single canonical control-flow order `detect_format → structured_bypass → normalize_format → rasterize → profiler → router → parse` was fixed (§13.1a), removing the xlsx-path ambiguity, and L3 profiling was made opt-in. Routing is document-level only; page/region-level mixed-protocol routing is a deferred open question. `ARCHITECTURE.md` §8 is a historical open-issues ledger; §14/§15 and in-section v0.9 notes are the latest corrections and win on conflict. Read `ARCHITECTURE.md` before implementing any top-level CLI code.

## Working in this repo

- When asked to compare approaches across parsers (e.g. "how does layout reconstruction work in MinerU vs. liteparse"), expect to read across multiple vendored repos; each has a very different architecture and language.
- `DEVELOPMENT_PLAN.md` breaks the `ARCHITECTURE.md` design into phased, gated tasks (P0–P10, task IDs `T-<phase>.<n>`). Check it before starting implementation work to see what phase/task you're in and what its Gate requires.

## `uparser/` — the unified CLI implementation (P0 done, P1 mineru-vlm validated against a real endpoint, P2/P3 dots.ocr/MonkeyOCRv2 offline-validated, P4 native fully working, P7 multi-format ingestion mostly working, P8 profiler+router+classify fully working, P5 pipeline protocol offline-validated with one confirmed environment gap, P6 paddleocr + reading-order fallback offline-validated, P9 cache/streaming/doctor/protocols/contract-tests fully working, P10 Node/Python bindings fully working and verified end-to-end, all adapters now genuinely reachable via `uparser parse --protocol <name>`)

A Rust workspace at the top-level `uparser/` directory (sibling to `opensource/`), implementing the design in `ARCHITECTURE.md`. Modeled directly on `opensource/liteparse`'s conventions (workspace shape, CLI+lib coexisting in one crate, reusing liteparse's `pdfium`/`pdfium-sys` crates by path dependency rather than re-wrapping PDFium FFI).

```
uparser/
├── Cargo.toml                        workspace root ([workspace.package] for shared edition/license)
├── .github/workflows/ci.yml          fmt / clippy / build+test
└── crates/
    ├── uparser-core/                 core library + `uparser` CLI binary
    │   ├── src/
    │   │   ├── types.rs                  unified IR: Block/Page/ParseResult/Geometry/CoordFrame (§5)
    │   │   ├── api.rs                     library-level `parse`/`classify` (P10/T-10.1/T-10.2), shared by `uparser-napi`/`uparser-python`
    │   │   ├── adapters/mod.rs           ProtocolAdapter trait (#[async_trait]) + `Registry` (factory closures keyed by name, not pre-built instances — `AdapterOverrides{endpoint,model}` lets `--endpoint`/`--model` reconfigure whichever adapter `--protocol` selects) + ParseCtx::dispatch (Real transport / Mock, §2.0/§2.1)
    │   │   ├── adapters/mock.rs          MockAdapter — used for the P0 CLI smoke test
    │   │   ├── adapters/mineru_vlm.rs    mineru-vlm two-stage adapter (layout→custom_token→per-block crop→content); **registered in `Registry::with_builtins()`, genuinely reachable via `uparser parse --protocol mineru-vlm --endpoint <url> --model <name>`** — real end-to-end run against the live MinerU2.5 endpoint confirmed correct text extraction post-fix (see below)
    │   │   ├── adapters/dots_ocr.rs      dots.ocr single-round adapter (smart_resize→one call→strict_json cells); registered/CLI-reachable same as mineru-vlm, but still has no real-endpoint validation of its own (none available)
    │   │   ├── adapters/monkeyocr_v2.rs  MonkeyOCRv2 two-stage adapter (LAYOUT call→python-literal cells→per-block recognition); same status as dots-ocr
    │   │   ├── adapters/native.rs        `#[cfg(feature = "native")]` — zero-model adapter embedding liteparse as a library; `parse_document()` is real/working; **CLI-reachable via `--protocol native`, which dispatches to `parse_document()` directly** (not through the scheduler — see `cli.rs::run_parse_native`, since `parse_page()` deliberately doesn't fit this adapter's whole-document shape)
    │   │   ├── transport.rs              reqwest+tokio OpenAI-compatible client, retry/backoff/semaphore
    │   │   ├── scheduler.rs              processing windows, concurrency budget, per-page failure isolation (§2.2); `run_streaming()` (P9/T-9.2) invokes a per-window callback for `--stream`'s incremental NDJSON output
    │   │   ├── cache.rs                   content-hash `ParseResult` cache (P9/T-9.1): sha256(bytes)+protocol+endpoint+model key, TTL, `get`/`put`/`stat`/`clear`
    │   │   ├── reading_order.rs           recursive XY-cut geometric reading-order fallback (P6/T-6.2), used directly by `paddleocr`/`pipeline`
    │   │   ├── ingest.rs                  rasterize() (pdfium-gated) + detect_format/structured_bypass/normalize_format/ingest_document (P7, unconditional — no feature gate, only the DOCX/PPTX/image conversion *paths* need LibreOffice/ImageMagick actually installed at runtime)
    │   │   ├── imaging.rs                 hard_resize/resize_by_need/crop/rotate_90n (mineru-vlm) + smart_resize (dots.ocr, Qwen2.5-VL-style factor/pixel-bound resize) + resize_by_pixel_bounds (MonkeyOCRv2, plain aspect-preserving sqrt-scale resize)
    │   │   ├── geometry.rs                denormalize_frac_bbox/denormalize_0to1000_bbox/IoU dedupe (mineru-vlm) + rescale_bbox_to_original (dots.ocr) + map_bbox_0to1000_clamped (MonkeyOCRv2's swap+clip+min-1px variant)
    │   │   ├── output_parse.rs            custom_token grammar (mineru-vlm) + strict_json fault-tolerant chain (dots.ocr) + python_literal_eval: hand-written Python-literal parser + bracket-scan tolerant extraction (MonkeyOCRv2, ported from `_parse_one_output` — never calls `eval`)
    │   │   ├── otsl.rs                    OTSL token sequence → HTML table (rowspan/colspan); shared **verbatim, unmodified since P1** by all three adapters — dots.ocr/MonkeyOCRv2 both emit OTSL for tables (dots.ocr can also passthrough literal HTML)
    │   │   ├── formula_repair.rs          LaTeX repair chain: balance_brackets/collapse_repeated_quad/normalize_tag_eqno/rebuild_unclosed_env; shared **verbatim, unmodified since P1** by all three adapters' Formula/equation handling (Gate G3 proof)
    │   │   ├── robustness.rs              is_degenerate() + retry_with_temperature() — this project's own robustness layer; still standalone/not wired into any adapter across all three protocols
    │   │   ├── category_map.rs            per-protocol native category → normalized category: `map_mineru_vlm_category` (21 lowercase) + `map_dots_ocr_category` (11 Title-Case) + `map_monkeyocrv2_category` (10 Title-Case, near-identical spelling to dots.ocr's) — same normalized target vocab from three protocols
    │   │   ├── postprocess.rs             pure-geometry paragraph merge; unmodified since P1 — proven protocol-agnostic against dots.ocr-shaped blocks too (Gate G2)
    │   │   ├── render/mod.rs             markdown/json/content-list renderers; unmodified since P0 — same Gate G2 proof
    │   │   ├── profiler.rs                profile_l1 (format-only, always available) + profile_l2 (`#[cfg(feature = "native")]`, reuses `liteparse::LiteParse::is_complex()` directly per ARCHITECTURE.md's explicit instruction — no new heuristics reimplemented)
    │   │   ├── router.rs                  route() implementing the §13.4 v1 routing table (document-level, always terminates via a fallback row)
    │   │   ├── cli.rs                     Agent-first CLI contract: exit codes 0-4, stdout=result/stderr=logs (§6.1); `parse` (`--protocol <name>|auto`, `--endpoint`, `--model`, `--no-cache`, `--stream`) + `classify`/`cache stat|clear`/`doctor`/`protocols` subcommands (P9); `rasterize_or_fallback()` (real PDF rasterization when `pdfium` is on, else a real-decoded-image page when the input is itself an image, else the P0 1x1 placeholder only as a last resort — see the bug note below) and `resolve_auto_protocol()` (Profiler+Router, printed to stderr)
    │   │   └── testing.rs                 MockDispatch test double for offline adapter tests
    │   ├── examples/mineru_vlm_smoke.rs   manual, network-dependent runner: `NO_PROXY=127.0.0.1,localhost cargo run --example mineru_vlm_smoke -- <page.png> [endpoint] [model]` — largely superseded by `uparser parse --protocol mineru-vlm` now that it's CLI-wired, kept as a lighter-weight alternative
    │   ├── examples/dots_ocr_smoke.rs     same pattern for dots.ocr — not yet run against a real endpoint (none available), ready for when one exists
    │   ├── examples/monkeyocrv2_smoke.rs  same pattern for MonkeyOCRv2 — also not yet run against a real endpoint
    │   └── tests/{fixtures/{raw,docs,golden}, cli.rs, contract.rs}
    ├── uparser-napi/                  real napi-rs Node.js bindings (P10) — `parse`/`classify` calling `uparser_core::api`, verified working via a real `node -e` smoke test
    └── uparser-python/                real PyO3 Python bindings (P10) — same two functions, verified working via a real `python3 -c` smoke test
```

Build/test/lint (run from `uparser/`): `cargo build --workspace`, `cargo test --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt --all -- --check`. All green by default with no extra flags/network access.

The `pdfium` feature (real PDF rasterization, via `liteparse-pdfium`) is **not** default-on: `liteparse-pdfium-sys`'s build script downloads a prebuilt PDFium binary from GitHub on first build, which isn't reliable in every environment. Opt in with `--features pdfium` when network access to GitHub releases (or a pre-populated `~/.cache/pdfium-rs`) is available; without it, `ingest::rasterize()` returns `IngestError::PdfiumFeatureDisabled`. **Confirmed working**: the download completes in ~2 minutes given enough time (past attempts that gave up at 90s were simply too impatient, not evidence the download is broken) — `cargo build --features pdfium`/`native` succeed and `ingest::tests::rasterizes_fixture_pdf` passes for real once `~/.cache/pdfium-rs` is populated. The `native` feature (§ below) pulls `pdfium` in transitively and is likewise opt-in, not default-on.

`T-0.1`–`T-0.9` (Gate G0) and `T-1.2`–`T-1.10` are implemented, and **Gate G1 is fully validated**: `adapters/mineru_vlm.rs` was run end-to-end (via `examples/mineru_vlm_smoke.rs`) against a real vLLM deployment of `MinerU2.5-2604-1.2B`, correctly extracting text/title/footer/table/image blocks (including OTSL→HTML table conversion) from real pages of a scanned 10-K filing. The mineru-vlm protocol details (custom-token grammar, image preprocessing, category vocab, OTSL/formula handling) were originally reverse-engineered from `mineru_vl_utils` **v0.1.14** (the only copy available locally; `opensource/MinerU/pyproject.toml` pins a newer `>=1.0.5` series not present on this machine) and confirmed largely accurate against the real endpoint, with one correction: **`skip_special_tokens: false` must be set explicitly in the request body** — vLLM's OpenAI-compatible endpoint defaults to stripping special tokens, which silently ate the `<|box_start|>`/`<|ref_start|>` wrapper tokens the `custom_token` grammar depends on (fixed in `layout_sampling()`/`stage2_prompt_and_sampling()`). `emitted_signals`/`provides_reading_order` (both `false` — no spans/merge_hint/reading-order signal observed from this backend) still hasn't been re-examined against real output and remains a possible deviation from `DEVELOPMENT_PLAN.md`'s assumption. `adapters/mineru_vlm.rs` is still not wired into `Registry::with_builtins()` or the CLI (no `--endpoint` flag exists yet) — that wiring, plus T-1.1's Python-prototype fixture capture, are the next steps before this protocol is user-facing.

`T-2.2`–`T-2.4` are implemented, and **Gate G2's offline-verifiable half is done**: `adapters/dots_ocr.rs` (single HTTP round-trip, strict-JSON output, no `spans`/`merge_hint` signal — structurally the opposite of mineru-vlm's two-stage flow) reuses `geometry.rs`/`category_map.rs`/`postprocess.rs`/`render/` via purely additive functions — `postprocess.rs` and `render/` got **zero** code changes, only new tests proving they already handle dots.ocr-shaped `Block`s identically to mineru-vlm-shaped ones. Unlike mineru-vlm, dots.ocr's protocol source is **fully vendored** in `opensource/dots.ocr` (no external-package version-mismatch risk), read directly from `image_utils.py`/`prompts.py`/`layout_utils.py`/`output_cleaner.py`/`inference.py`. One confirmed deviation from `DEVELOPMENT_PLAN.md`'s assumption: dots.ocr's bbox coordinates are **plain absolute pixels of the model's resized input image**, not a `Norm0To1000` normalized space — `coordinate_system()` returns `PixelAbs` and `parse_page` does the resized→original pixel conversion internally via the new `geometry::rescale_bbox_to_original`. No dots.ocr vLLM endpoint has been available to validate end-to-end (unlike mineru-vlm) — `examples/dots_ocr_smoke.rs` exists and is ready to run the moment one does; until then, treat this adapter's fidelity as offline-verified only.

`T-3.2`–`T-3.4` are implemented, and **Gate G3 is done**: `adapters/monkeyocr_v2.rs` (`opensource/MonkeyOCRv2`, fully vendored — not the v1 SRR-triplet code in `opensource/MonkeyOCR`) is a third, structurally distinct protocol (its own resize strategy, its own Python-literal-list raw output format requiring a hand-written recursive-descent parser since the real service parses via `eval()` and JSON won't cover single-quoted strings) that reuses `otsl.rs` and `formula_repair.rs` **completely unmodified** — verified both by the fact that this session never edited those two files and by an explicit adapter-level test asserting the shared `formula_repair::DEFAULT_CHAIN` correctly repairs a malformed formula inside the MonkeyOCRv2 pipeline. `category_map.rs` gained a third mapping function (`map_monkeyocrv2_category`, 10 Title-Case categories, near-identical spelling to dots.ocr's vocab) and `geometry.rs`/`imaging.rs`/`output_parse.rs` each gained one more additive, protocol-specific function — the shared/reused-vs-new split is exactly what Gate G3 was designed to measure. Document dewarming preprocessing (an independent local-torch model in the real MonkeyOCRv2 pipeline) is deliberately not implemented, per this phase's plan (option a: skip). No MonkeyOCRv2 vLLM endpoint has been available — `examples/monkeyocrv2_smoke.rs` exists, unrun, same as dots.ocr's.

`T-4.1`–`T-4.3` are implemented (`adapters/native.rs`, `#[cfg(feature = "native")]`): a **zero-model** protocol embedding `opensource/liteparse` (a first-party Rust library, not a wire protocol — no reverse-engineering needed) via `LiteParse::new(LiteParseConfig { ocr_enabled: false, .. }).parse_input(PdfInput::Bytes(..))`. Unlike P1–P3, this adapter's core method, `parse_document()`, is **tested against a real fixture PDF and genuinely works** (`ocr: 0.0ms` in liteparse's own timing log confirms OCR is correctly disabled; `extract: 36.3ms` shows real text-layer extraction) — no mocking needed since there's no network dependency, only the already-solved pdfium binary availability. One deliberate, documented trait-shape mismatch: `ProtocolAdapter::parse_page()` (the per-rasterized-page, per-request contract every other adapter implements) doesn't fit a library that parses a **whole document** in one call with its own internal PDFium/memory management — `NativeAdapter::parse_page()` is implemented but returns an explanatory `PageError`; the real entry point is the inherent (non-trait) `parse_document(source_path, pdf_bytes)`. Reconciling this with `scheduler.rs`'s per-page pipeline is deferred, likely a P8/router-time concern. `RawOutputFormat` gained a `None` variant (first change to that enum since P0) for this zero-wire-format case. `native` pulls in `pdfium` transitively and is opt-in, not default-on, same posture as `pdfium` itself.

`T-7.1`–`T-7.4` are implemented in `ingest.rs` (unconditionally, no feature gate — `detect_format`/`structured_bypass`/`normalize_format`/`ingest_document`, wired in the canonical `detect → structured_bypass? → normalize → (rasterize, separate)` order fixed by `ARCHITECTURE.md` §13.1a). `detect_format` uses the `file-format` crate (the same one `opensource/liteparse` already depends on for this) plus a filename-extension fallback for CSV (which has no reliable magic-byte signature). `structured_bypass` reads XLSX (`calamine`) / CSV (the `csv` crate — calamine 0.26 turned out **not** to support CSV despite the P7 plan's assumption it did via one shared `Reader` trait; a real, caught-and-corrected mistake, not a documented deviation) directly into `Block`s with `source = BlockSource::StructuredNative` (declared in P0's IR, unused until now) — **genuinely no model call, no rasterization**, fully tested with real CSV bytes (XLSX's zip-based binary format isn't hand-constructed in a unit test, a known gap). `normalize_format` shells out to `soffice`/`magick` with a bounded `tokio::time::timeout`, distinguishing `ToolNotFound` (hard env problem) / `ConversionTimedOut` (maybe retry-worthy) / `ConversionFailed` (tool ran, rejected input) — **fully tested for the failure paths** (missing-binary and timeout, via an overridable `ToolNames` struct so tests don't depend on `soffice`/`magick` actually being absent) since neither LibreOffice nor ImageMagick is installed in this environment; the real conversion *success* path (T-7.3's actual DOCX/PPTX/image→PDF output) remains unverified until those tools are available, and T-7.5 (perf/failure-rate evaluation) is documented as a design-and-degradation-strategy note on `normalize_format()` rather than measured real timing, for the same reason. `otsl::escape_html` was made `pub(crate)` and reused directly from `ingest.rs` — the first real cross-module code reuse beyond the adapter-level otsl/formula_repair sharing from P1–P3.

`T-8.1`–`T-8.3` are implemented, and **this phase's core claim is genuinely verified end-to-end**: `uparser classify <fixture.pdf>` (with `--features native`) prints a real `DocumentProfile` with `dominant_content: "text_dominant"`, `kind: "report"`, `kind_confidence: 0.8` for a real digitally-native PDF — the exact "digitally-native long text → native" split T-8.1's acceptance criterion asks for, validated against real liteparse output, not a mock. `types.rs`'s P0-era `DocumentProfile` stub (`{ notes: Vec<String> }`) was replaced outright with the full `ARCHITECTURE.md` §13.3 shape (`DocumentKind`/`ContentMix`/`ProfileLevel`/`PageProfile`/`DocumentProfile`, plus `TableSubtype`/`ChartSubtype` declared for shape-compatibility though genuinely unused — they're L3-only). `profiler.rs`'s L2 layer calls `liteparse::LiteParse::is_complex()` directly (not reimplemented) per the architecture doc's explicit instruction, mapping its `PageComplexityStats`/`LayoutComplexityStats` into our `PageProfile` — `has_chart_region` is an honest proxy (`figure_coverage >= 0.2 && !has_table_region`, reusing liteparse's own `DENSE_GRAPHICS_MIN_COVERAGE` constant), not a confident chart classifier, since real chart detection is L3-only. `router.rs::route()` implements the §13.4 table in order with an unconditional fallback row (never panics); its `"pipeline"` protocol recommendation is a string label only — no `pipeline` adapter exists yet (P5's job). T-8.4 (`--protocol auto`) was originally deferred pending adapter registration but **is now done** (see below — it landed as part of the CLI-wiring pass, not a separate phase). T-8.5 (opt-in L3) remains deferred — needs a real model-call endpoint, the same category as every VLM adapter's real-endpoint gap.

**Post-P8: all adapters wired into the CLI for real** (`--protocol mineru-vlm`/`dots-ocr`/`monkeyocr-v2`/`native`/`auto` are now genuinely usable, not just reachable via `examples/*_smoke.rs`). `Registry` was refactored from storing pre-built `Arc<dyn ProtocolAdapter>` instances to storing **factory closures** (`AdapterOverrides { endpoint, model }` applied inside the closure) — the minimal change that lets `--endpoint`/`--model` reconfigure whichever adapter gets selected without needing `Any`/downcasting on the trait object. `cli.rs::run_parse` gained a `native`-specific branch (dispatches straight to `parse_document()`, bypassing the scheduler entirely, since that adapter's whole-document shape doesn't fit `parse_page()`) and `--protocol auto` (runs `profiler`+`router`, logs the choice to stderr, substitutes the result into the same flow — this is T-8.4, done here rather than in P8 since it depends on the registry change this pass made anyway). **A real bug was caught and fixed by this session's own end-to-end verification**, not just by unit tests: `rasterize_or_fallback()`'s non-`pdfium` fallback originally hardcoded `width: 1, height: 1` regardless of whether the input was actually a decodable image — silently correct for `mock`/garbage-byte tests, but for a real adapter this corrupts every bbox denormalization (which divides by page width/height), and a live run against the real MinerU2.5 vLLM endpoint (the same one validated in P1, confirmed still reachable) came back with every block reading literally `"[Non-Text]"` before the fix. Fixed by decoding the input directly via the `image` crate when it's a real image, falling back to the `1x1` placeholder only for genuinely non-image bytes; the same live endpoint then produced correct real text (`"UNITED STATES"`, `"SECURITIES AND EXCHANGE COMMISSION"`, etc.) with correct `1275x1651` page dimensions. `dots-ocr`/`monkeyocr-v2` are now equally CLI-reachable but still lack their own real-endpoint validation (no endpoint available for either).

**P5 (`pipeline` protocol + stage backends, T-5.1/T-5.3–T-5.6 implemented, T-5.7 deferred)**: adds `adapters/pipeline.rs`, first real consumer of P1's until-now-unused `ModelStage`/`StageBackend`/`ResourceHint` types. Per `ARCHITECTURE.md` §11.2 (confirmed against `opensource/MinerU/mineru/utils/enum_class.py`'s `BlockType` and `model_init.py`): `layout`(PP-DocLayoutV2)/`ocr`(PaddleOCR-torch)/`formula`(Unimernet) are all torch models with no confirmed ONNX export, so they default `Remote`, `allows_local: false` — a `Remote`-stage failure is always a loud per-block `PageError`, never a silent local fallback (§11.4; `unreachable_remote_ocr_stage_surfaces_as_block_error_not_silent_fallback` test proves this), and passing `--layout-backend local`/`--ocr-backend local`/`--formula-backend local` is rejected as a CLI usage error (exit 1) before an adapter is even built, since there's genuinely nothing to switch to. `table`(SLANet-Plus/UnetTableModel) is the one stage MinerU already loads as ONNX, so it's the only stage `allows_local: true` (default `Local`). New modules: `adapters/pipeline_serving.rs` (Pipeline Model Serving's REST contract — `LayoutStageRequest/Response`, `OcrStageRequest/Response`, `FormulaStageRequest/Response`, `TableStageRequest/Response`; a **new, custom, non-chat-completions shape**, since the three Remote stages don't talk an OpenAI-compatible API), `adapters/onnx_table.rs` (`ort`-crate-backed local table inference, gated behind a new non-default-on `pipeline-local-table` feature — same posture as `pdfium`/`native`). `transport.rs` gained `Transport::dispatch_rest`/`RestRequest` (a sibling to the existing chat-completions `dispatch`, sharing the same retry/backoff/concurrency skeleton via a new private `post_with_retry` helper) and `ParseCtx::dispatch_rest` (same `Dispatcher::Mock`-keyed-by-endpoint-string mechanism as `dispatch`, zero changes needed to `MockDispatch` itself — the four-stage orchestration is fully offline-tested this way, same pattern as every prior adapter). `category_map.rs` gained `PIPELINE_LAYOUT_CATEGORIES`/`map_pipeline_category`, confirmed from `enum_class.py`'s "Added in pp_doclayout_v2" `BlockType` subset (`doc_title`/`paragraph_title`/`text`/`abstract`/`image`/`table`/`chart`/`interline_equation`/`list`/`index`/`header`/`footer`/`page_number`/`footnote`/`vertical_text`/`discarded`). `RawOutputFormat::OcrBoxes` (declared unused since P0) got its first real consumer. `AdapterOverrides` gained an optional `pipeline: Option<PipelineConfig>` field (`None` for every other adapter) carrying per-stage endpoint/backend overrides; `cli.rs`'s `Command::Parse` gained `--layout-backend`/`--layout-endpoint`/`--ocr-backend`/`--ocr-endpoint`/`--formula-backend`/`--formula-endpoint`/`--table-backend`/`--table-model-path` (T-5.1). `provides_reading_order = false` (MinerU's `para_split` isn't ported; deferred to P6's `reading_order.rs` geometric fallback, same as PaddleOCR's future adapter). **Confirmed real environment gap** (not a code defect, documented in both `Cargo.toml`'s `pipeline-local-table` feature comment and `adapters/onnx_table.rs`'s module doc): in this sandbox (Ubuntu 22.04, glibc 2.35), `cargo build --lib --features pipeline-local-table` compiles cleanly, but linking any binary/test target fails — `ort`'s `download-binaries`-fetched prebuilt ONNX Runtime static archive references glibc ≥2.38 symbols (`__isoc23_strtoll` and friends) this sandbox's glibc doesn't provide; switching to `ort`'s `load-dynamic` feature avoids the static link but then hangs fetching the dynamic-library variant from `ort.pyke.io` (confirmed via a 30s hard-timeout probe — a host the corporate proxy likely can't reach). Same category of gap as P1's `mineru_vl_utils` version pin or P4's original pdfium-download caveat. `onnx_table.rs`'s tests (round-trip against a synthetic identity-graph ONNX fixture at `tests/fixtures/synthetic_identity.onnx`, generated via the local `onnx` Python package — not real SLANet weights, which aren't vendored anywhere in `opensource/MinerU` either) are expected to build and pass unmodified on a host with glibc ≥2.38; they have not been run to green in this sandbox. Everything else — the four-stage `MockDispatch`-offline orchestration test, the CLI wiring/usage-error tests, `category_map`/`pipeline_serving` roundtrip tests — passes for real (176 unit + 11 CLI tests, default features). T-5.7 (a reference Pipeline Model Serving deployment) remains deferred — needs MinerU's real torch weights, not vendored here; Gate G5's CPU/memory measurement is likewise not empirically measured, same reasoning.

**P6 (PaddleOCR + reading-order fallback, T-6.1–T-6.4 all implemented)**: adds `reading_order.rs` (new, shared) and `adapters/paddleocr.rs`. `reading_order.rs::assign_reading_order()` is a from-scratch recursive XY-cut (projection-profile splitting: find a clean horizontal or vertical gap partitioning the block set into non-overlapping bands, recurse into each; a genuinely tangled/overlapping set falls back to sorting by `(y0, x0)`) — deliberately **not** a port of `opensource/liteparse`'s `projection.rs` (~5500 lines operating on liteparse's own per-character text items, a different problem shape from ordering already-detected `Block` bboxes) and has no `native`-feature dependency, since both its consumers (`paddleocr`, `pipeline`) are unconditional core adapters, not feature-gated. **A real design decision surfaced by its own test suite**: the two candidate split axes must be tried **column-first, then row** (not the other, more "natural-sounding" order) — a two-column layout's stacked left/right blocks are geometrically ambiguous between "read column-by-column" and "read row-by-row" from bboxes alone, and only column-first priority produces the correct left-column-then-right-column order a real multi-column document needs; this was caught by `two_column_layout_reads_left_column_then_right_column` actually failing on the first (row-first) implementation, not assumed correct upfront. T-6.1 (PaddleOCR/PaddleX service-contract confirmation, `ARCHITECTURE.md` §8) has the same "no real deployment to reverse-engineer against" gap as P5's Pipeline Model Serving — PaddleX/PaddleServing/PaddleOCR's own HTTP demo are all real options with no single canonical schema, so `adapters/paddleocr.rs` documents and implements its own minimal, reasonable REST contract (`PaddleOcrRequest`/`PaddleOcrResponse`, reusing `pipeline_serving::StageImage`'s base64-PNG shape) rather than guessing at an unconfirmed real one, offline-tested via `MockDispatch` same as every prior adapter. T-6.3 (`Geometry::Polygon` handling) turned out to need **no new postprocess/render code at all**: `postprocess.rs::merge_paragraphs_by_geometry` already only reads `bbox_px` (never `geom` directly), and `geometry::geometry_bounds()` (declared back in an earlier phase, previously unused) already computes a polygon's bounding rect — so `paddleocr.rs` just calls it when building blocks, and a new test (`merges_polygon_shaped_paddleocr_blocks_via_their_bounding_rect`) proves polygon-sourced blocks merge identically to rect-sourced ones without any special-casing; `render::to_json`'s existing serde derive already round-trips `Geometry::Polygon` losslessly (no change needed there either). `category_vocab()` returns a single-element `&["text"]` (T-6.4's "无分类映射为 text" — there is no native layout vocabulary to map from at all, unlike every other adapter's `category_map.rs` function). **`pipeline.rs` was also retrofitted** to call `reading_order::assign_reading_order` directly (previously left `reading_order: None` per the P5-era plan, deferred exactly to "P6") — both `paddleocr` and `pipeline` declare `provides_reading_order() = false` (honestly describing that neither protocol's own model output carries an order signal) but call the fallback function directly inside `parse_page` rather than waiting on `scheduler.rs` to generically wire it (a deferred gap, same category as `postprocess.rs` never having been scheduler-wired since P1). A pre-existing `BlockSource` mismatch was also caught and fixed in passing: `pipeline.rs`'s blocks were tagged `BlockSource::OcrPipeline` (P5), but that variant's documented intent is a no-layout detect+recognize flow (PaddleOCR's actual shape) — `pipeline` (which does have a distinct layout stage) now correctly uses `BlockSource::LayoutThenRecognize`, the same variant mineru-vlm uses for its structurally-identical two-stage shape. All new tests pass for real: 186 unit + 12 CLI tests (default features), 193 unit + 11 CLI tests (`--features native`) — no environment gaps this phase, unlike P5.

**P9 (cache + streaming + doctor + protocols + contract tests, T-9.1–T-9.6 all implemented, sub-key layering deferred)**: adds `cache.rs` (T-9.1) — content-hash keyed (`sha256(source bytes) + protocol + endpoint + model` via `ParamFingerprint`/`cache_key()`), storing full `ParseResult`s as one JSON file per entry under `<base_dir>/<hash[..2]>/<hash>.json` with a TTL (default 24h) and `get`/`put`/`stat`/`clear`. As predicted back in the P5-era plan text, this module needed **no external dependency and landed with full fidelity** — genuinely real, not offline-scoped like P5's `ort`/P1's endpoint gaps. §15's "profiler intermediate artifact sub-key" half is explicitly **not** built: nothing in this codebase currently produces a rasterized-page/thumbnail artifact as an independently cacheable value (`profiler.rs`'s `profile_l2` computes straight from raw bytes in one `liteparse::is_complex()` call), so that layer is deferred until a real intermediate exists to key on rather than speculatively caching a shape nothing consumes. `cli.rs` gained `--no-cache`, `--stream`, and three new subcommands: `cache stat|clear`, `doctor <protocol> [--endpoint]`, `protocols`. `scheduler.rs` gained `run_streaming()` (T-9.2) — same windowed/concurrency-bounded execution as `run()`, but invokes a per-window callback as soon as that window completes (verified via a test asserting window sizes `[4, 4, 2]` for 10 pages at window_size=4) so `--stream` can emit NDJSON incrementally instead of building one aggregate `ParseResult`; `native`'s whole-document path doesn't participate in caching/streaming (it bypasses the scheduler entirely per its P4-era design, and has no network round-trip to amortize — the cost caching exists to avoid). `doctor` (T-9.3) does a real reachability probe (a GET against the target/default endpoint — any HTTP response, even 404/405, counts as "reachable"; only a transport-level failure counts as unreachable) for HTTP-backed protocols, and for `pipeline` reports real local `std::thread::available_parallelism()` CPU count + `/proc/meminfo`'s `MemAvailable` (no new `sysinfo`-style dependency) as a non-binding Local/Remote advisory. `protocols` (T-9.4) required one small `Registry::names()` addition and otherwise just introspects every built-in adapter's existing trait methods into JSON. **A real test-isolation bug was caught and fixed while wiring this up**: `cache::default_cache_dir()` unconditionally resolved to the real `$HOME/.cache/uparser`, so running the CLI test suite was silently writing/reading real cache entries on the development machine — several tests share the literal bytes `b"fake pdf bytes"` + protocol `mock`, which would hash to the *same* cache key and risk cross-test collisions under parallel test execution. Fixed by adding a `UPARSER_CACHE_DIR` env var override (checked first in `default_cache_dir()`), with every cache-touching CLI test now passing its own isolated `tempfile::tempdir()` via `.env(...)`. T-9.5/T-9.6 (contract tests / multi-protocol consistency) landed as a new `tests/contract.rs`: rather than duplicating the per-adapter offline tests already covering most of T-9.5's individual scope (two-stage orchestration, malformed-output recovery, partial-page failure isolation — present in every adapter's own `#[cfg(test)]` module since P1) or the `insta` snapshot tests `render.rs` already had since P0, it adds the genuinely new piece: feeding mineru-vlm and MonkeyOCRv2 (two structurally different two-stage protocols, different native wire grammars) equivalent mocked layout+content responses and asserting their rendered Markdown/content-list output agrees exactly despite each protocol's own `category_raw` spelling (`"text"` vs `"Text"`) — a real cross-protocol regression net a single adapter's tests can't provide. All new/changed tests pass for real: 199 unit + 19 CLI + 2 contract tests (default features), 206 unit + 18 CLI tests (`--features native`) — no environment gaps this phase.

**P10 (language bindings, T-10.1/T-10.2 implemented and verified end-to-end; T-10.3/T-10.4 scoped down)**: this environment turned out to have working `node`/`npm` (v24.18.0, via `registry.npmmirror.com`), `python3`/`pip3` (3.12.7, via a Tsinghua PyPI mirror), and both mirrors were confirmed genuinely reachable — so unlike P5's `ort`/glibc gap, real bindings were buildable and testable here, not just structurable. Added `src/api.rs`: `pub async fn parse(path, &ParseOptions) -> Result<ParseResult, ApiError>` and `pub async fn classify(path) -> Result<DocumentProfile, ApiError>`, the shared library-level entry points both binding crates call. **Known, documented duplication** (stated in `api.rs`'s own module doc, not hidden): `cli.rs::run_parse` keeps its own independent implementation of the same core logic (registry build, cache, rasterize, scheduler dispatch) rather than `api.rs` being extracted from it — `cli.rs` additionally owns `--stream`'s incremental-NDJSON and CLI-specific exit-code/stderr formatting, which have no business in a library API the bindings call for one-shot request/response use; fully unifying the two was judged higher-risk (touching already-tested CLI control flow) than valuable for this pass, and is left as an explicit follow-up. `uparser-napi` (real napi-rs bindings, `napi 2.16`/`napi-derive 2.16`, replacing the placeholder) exports `async fn parse(path, options?) -> String` / `async fn classify(path) -> String`, both returning `serde_json::to_string(&result)` — the exact same JSON `ParseResult`/`DocumentProfile` serialization the CLI's `--format json` produces, so IR consistency across surfaces holds *by construction* (one `Serialize` impl, three callers) rather than by parallel-implementation discipline. `uparser-python` (real PyO3 bindings, `pyo3 0.29` — matching `opensource/liteparse`'s own proven pin in this same environment, chosen over `0.22` specifically because `0.22`'s macro-generated code hits a `rust_2024_compatibility` `unsafe_op_in_unsafe_fn` warning under this workspace's edition-2024 setting that `0.29` doesn't) exports `parse(path, protocol=None, ...)` / `classify(path)` module functions the same way; since PyO3 has no napi-rs-simple async-fn story (would need `pyo3-async-runtimes` + an asyncio bridge), both block on a lazily-initialized `OnceLock<tokio::runtime::Runtime>` — the same `Runtime::block_on` pattern `cli.rs` itself already uses for every command, not a new pattern introduced just for this. **Both bindings were verified for real, not just compiled**: built the `uparser-napi` cdylib in release mode, copied it to a `.node` file, and called `classify()`/`parse()` from a real `node -e` script — got back correct `DocumentProfile`/`ParseResult` JSON; separately built `uparser-python`'s cdylib (`lib_uparser.so`, named for its `crate-type`/`[lib] name = "_uparser"` configuration matching the `#[pymodule] fn _uparser` entry point), imported it directly in `python3` (bypassing `maturin develop`'s venv machinery, still a genuine native-extension load), and called the same two functions — output byte-identical to the Node run for the same input file, confirming Gate G10's "three surfaces call the same core, IR serialization consistent" claim empirically, not just architecturally. **T-10.3 (`packages/node`/`packages/python` npm/pip wrapper scaffolding — prebuild scripts, N-API target triples, maturin wheel builds) and T-10.4 (release docs/binary-size accounting) are explicitly not built this pass**: they're packaging/distribution concerns orthogonal to "do the bindings work," `opensource/liteparse`'s own `packages/` structure is available as a direct template for T-10.3 when that's prioritized, and this repo has stated no CI/release pipeline to target yet. `cargo build --workspace`/`--features native`, full test suite (205 unit + 19 CLI + 2 contract, default; 212 unit + 18 CLI + 2 contract, `--features native`), `clippy --all-targets`, and `cargo fmt --check` all pass clean with both binding crates in the workspace.

**Post-P10: a real production-blocking deadlock was found and fixed via the user's own manual live testing** (`--protocol mineru-vlm` against the real `MinerU2.5-2604-1.2B` vLLM endpoint on a genuine 7-page PDF) — `uparser parse` hung indefinitely (20+ minutes, zero CPU/network activity, no error, no timeout) on any real multi-page document once `mineru-vlm` reached its per-block content-recognition stage. Root cause: `scheduler.rs::run`/`run_streaming` acquired a permit from the document-level concurrency semaphore (`--max-concurrency`, default 4) **for the whole `parse_page()` call**, held for the page's entire duration — but `mineru-vlm`/`pipeline`/`monkeyocr-v2` *also* acquire their own permit from that same semaphore per content-recognition block inside `parse_page()`. Once `max_concurrency` pages were running concurrently (trivially true for any document with more than a few pages), every permit was held by an outer page-level guard, so none of those pages' own inner per-block `acquire_permit()` calls could ever succeed — total deadlock, each page waiting on a permit only another equally-stuck page could release. **Why 200+ existing tests never caught this**: `scheduler.rs`'s own tests used adapters with no internal permit acquisition (no nesting); every adapter's own unit tests call `parse_page()` directly, bypassing the scheduler (no outer permit); the CLI integration tests that *do* exercise the real scheduler+adapter path point at a refused port, so they fail at stage 1 (layout, which needs no permit) before ever reaching the block-level stage 2 where the nesting bites — no test combined "real scheduler-level page concurrency" with "real adapter-level block concurrency" until a real multi-page document forced it. **Fix**: removed the outer page-level `acquire_permit()` from both `scheduler.rs` methods (the correct throttling granularity is per-network-request, which the three affected adapters already implement internally — the scheduler acquiring one too was both redundant and the actual cause of the deadlock); added a regression test, `many_concurrent_pages_with_per_block_permits_does_not_deadlock`, that reproduces the exact nested-acquisition shape via a `NestedPermitAdapter` and asserts completion within a 10s `tokio::time::timeout` — verified this test genuinely catches the bug by temporarily reintroducing the outer acquire and confirming the test fails (times out) before re-applying the fix. Re-ran the same real 7-page document end-to-end post-fix: 21.5s (cold) / 21.1s (`--no-cache`, confirming it's not a fluke) / 0.03s (cache hit) — down from an unbounded hang. Also fixed a smaller, related finding surfaced by the same live run: mineru-vlm's live `MinerU2.5-2604-1.2B` checkpoint emits a `list_item` category not present in the `mineru_vl_utils` v0.1.14 vocab `category_map.rs` was built against (a real newer-checkpoint vocabulary drift, same category of gap as the v0.1.14 pin itself) — added `"list_item"` to `MINERU_VLM_CATEGORIES` and `map_mineru_vlm_category`, mapping it to `"list"` alongside the existing `"list"` entry; confirmed the `unrecognized mineru-vlm category` warning is gone on a fresh (`--no-cache`) re-run of the same document. All tests pass post-fix: 207 unit + 19 CLI + 2 contract (default features), `clippy`/`fmt` clean.

**Post-P10 continued: deep source-level audit (four parallel focused reviews covering all ~25 source files) + first implementation batch.** Per-module review surfaced 35+ concrete findings (concurrency risks, network retry gaps, VLM-output correctness bugs, reading-order edge cases, CLI/IR inconsistencies) written up in `CLI_ENHANCEMENT_PROPOSAL.md`. The three highest-priority findings — each a "correct code that was never wired into any real call path" gap, the same class of bug as this session's scheduler deadlock — were then implemented:

1. **`postprocess::merge_paragraphs_by_geometry` wired into both real call paths** (`api.rs::parse`/`cli.rs::run_parse`), applied per-page to `scheduler.run()`/`run_streaming()`'s output before rendering; new `--no-postprocess`/`ParseOptions::no_postprocess` flag bypasses it for debugging raw-vs-merged diffs. **A real testing-technique problem was fixed alongside this**: `MockAdapter` previously emitted one hardcoded block per page, which meant merge behavior could never be proven through the real registry-selected `mock` protocol — it now emits two adjacent, same-category, geometrically-mergeable blocks by design (documented in `mock.rs`), letting `parse_applies_postprocess_by_default`/`parse_no_postprocess_returns_raw_unmerged_blocks` (api.rs) and an equivalent CLI integration test prove the wiring end-to-end rather than only re-testing the merge function in isolation (which was already green the whole time postprocess sat unused — proof that testing the function isn't the same as testing the integration).
2. **`ingest::structured_bypass`/`normalize_format` wired into both real call paths.** XLSX/CSV now short-circuit unconditionally before protocol/cache logic (matching §13.1a's fixed control-flow order) — previously a `.csv` file would fail `image::load_from_memory`, fall to the 1×1 placeholder, and get fed to a protocol adapter as a blank image with **exit 0 and no error**, the single most severe finding from the audit. DOCX/PPTX now route through real `normalize_format` (LibreOffice) + a new `ingest::rasterize_pdf_bytes()` (writes converted bytes to a temp file since `pdfium::Library::load_document` only reads from a path, not bytes) instead of also silently degrading. Verified for real: `parse_bypasses_csv_to_structured_result_regardless_of_protocol` proves XLSX/CSV bypass wins over whatever `--protocol` was requested (calamine/csv crates need no external tool, fully real); `parse_docx_without_libreoffice_installed_is_a_clean_ingest_error` proves DOCX now reaches genuine `normalize_format` dispatch logic and fails with a clean `IngestFailed`/dependency-exit-code error (not a panic, not a silent wrong result) — required constructing a real minimal DOCX fixture via the `zip` crate (newly added as a dev-dependency) after an initial attempt using a bare `PK\x03\x04` magic-byte prefix was confirmed (by the test itself failing) insufficient for the `file-format` crate's actual OOXML sniffing, which requires a genuinely parseable zip with a `word/`-prefixed entry.
3. **`ParseResult.warnings` is now genuinely populated.** `ParseCtx` gained a `warn()` method backed by a shared `Arc<Mutex<Vec<String>>>` (new `ParseCtx::new_with_shared_warnings` constructor, used only by `scheduler.rs`; the existing `new`/`with_mock` constructors are unchanged and unaffected, avoiding a breaking-signature-change ripple across ~20 existing test call sites) — `warn()` both `eprintln!`s immediately (unchanged live-CLI visibility) and collects into the shared sink. `scheduler.rs::run`/`run_streaming` both gained a third return value (`Vec<String>`) aggregating every page's warnings; `run_streaming`'s `on_window` callback signature grew a matching `&[String]` parameter so `--stream`'s NDJSON lines also carry `window_warnings`, not just the final aggregate. All four real adapters' `eprintln!`-only category-mapping/output-recovery warnings (`mineru_vlm.rs`, `dots_ocr.rs`, `monkeyocr_v2.rs`, `pipeline.rs` — `paddleocr.rs` has none, single fixed category) now go through `ctx.warn()` instead. Verified for real via a new wiremock-backed CLI test, `unrecognized_category_warning_surfaces_in_parse_result_warnings`: a fake mineru-vlm endpoint returns a layout box with an invented unrecognized category, and the resulting `uparser parse` JSON output's `"warnings"` array is asserted to contain the fallback message — proving the full vertical (adapter → `ctx.warn()` → scheduler collection → `ParseResult.warnings` → CLI JSON) works end-to-end through the real binary, not just at the unit level (a dedicated `scheduler.rs` test, `run_collects_warnings_from_every_page`, also proves the collection mechanism itself across multiple concurrent pages).

All three changes verified together against the same real `MinerU2.5-2604-1.2B` endpoint on the same real 7-page PDF used to find/fix the deadlock: 21.3s end-to-end (cold), `page_errors: []`, `warnings: []` (the `list_item` category is now recognized so there's nothing to warn about on this particular document), and the rendered Markdown is byte-identical to the pre-change baseline — confirming `postprocess` ran safely as a no-op (mineru-vlm's layout detection already emits full-paragraph blocks, nothing adjacent to merge) rather than silently corrupting already-correct output. Full test suite: 212 unit + 23 CLI + 2 contract tests (default features), 219 unit + 22 CLI + 2 contract (`--features native`), `clippy --all-targets` and `cargo fmt --check` both clean. New dev-dependency: `zip = "2"` (for constructing real DOCX test fixtures — already a transitive dependency via `calamine`, now direct).

**Post-P10 continued, second implementation batch (B.1/B.4/B.5/B.2 from `CLI_ENHANCEMENT_PROPOSAL.md`'s deep audit — concurrency/resource risk convergence).**

- **B.1 — panic on one page no longer aborts the whole `run`/`run_streaming` call.** `scheduler.rs` previously did `handle.await.expect("adapter task panicked")`, which re-panicked (and discarded every other already-completed page's result) if any single page's adapter task panicked — a real gap distinct from the existing page-level `Result::Err` isolation, and adapters process untrusted, possibly malformed model output, so this was a plausible real-world trigger, not just theoretical. Fixed by pairing each spawned task with its `page_num` and converting a `JoinError` into a `PageError` (new `page_panic_error()` helper) instead of propagating it — same pattern in both `run` and `run_streaming`. New regression test `panic_on_one_page_is_isolated_not_fatal_to_the_whole_run`: a `PanicOnPageAdapter` panics on page 3 of 5, asserting the other 4 pages still come back successfully and page 3 surfaces as a normal `PageError`.
- **B.4/B.5 — LibreOffice/ImageMagick subprocess hygiene.** Two real gaps in `ingest.rs`'s conversion helpers: (1) a timed-out conversion left the real `soffice`/`magick` process running as an orphan — `tokio::time::timeout` dropping the `cmd.output()` future only abandons *our* handle, it doesn't kill the child by default. Fixed by centralizing `cmd.kill_on_drop(true)` inside `run_with_timeout()` itself (rather than at each of the two call sites, so no future third caller can forget it). **Verified for real, not just plausible**: new test `kill_on_drop_actually_terminates_the_child_on_timeout` spawns a real `sleep 5`, times out waiting on it after 100ms, drops the child, and asserts `/proc/<pid>` no longer exists — confirmed this test genuinely catches the regression by temporarily removing `kill_on_drop` and observing the test fail with the process still alive, exactly the orphan-process bug this fixes. (2) LibreOffice serializes concurrent headless invocations on a shared per-user profile lock, so a second concurrent DOCX/PPTX conversion (a realistic batch scenario) would likely hang or fail waiting on the first's lock. Fixed by passing a unique `-env:UserInstallation=file://<tempdir>/profile` per invocation, reusing the same per-call unique tempdir the input/output files already live in — no new resource, just one more arg. (LibreOffice/ImageMagick still aren't installed in this sandbox, so the real conversion *success* path — same caveat as P7 — remains verified only via the `ToolNotFound` failure path and this subprocess-hygiene fix's own general mechanism test, not an actual successful DOCX→PDF conversion.)
- **B.2 — permit acquisition moved to wrap only the network dispatch, not the CPU-bound crop/rotate/resize/base64-encode preceding it.** `mineru_vlm.rs`/`monkeyocr_v2.rs`/`pipeline.rs`'s stage-2/stage-fan-out loops all previously acquired their per-block `ctx.acquire_permit()` *before* doing this image work, meaning the document-level concurrency budget (`--max-concurrency`) was partly consumed by synchronous CPU work with no request in flight — silently reducing real network concurrency below the configured value. Moved the acquire to immediately before each adapter's `ctx.dispatch()`/`ctx.dispatch_rest()` call in all three files; for `pipeline.rs` specifically (whose fan-out closure delegates to `recognize_text`/`recognize_formula`/`recognize_table` methods that build+dispatch their own requests internally), the permit now lives inside each of those three methods right before their own `dispatch_rest()` call, rather than at the outer fan-out call site — the `Local` table backend path (no network request) correctly never acquires a permit at all now. B.3 (extracting a shared `fan_out_stage2` helper to de-duplicate the ~35-line orchestration skeleton copy-pasted three times — the audit's other half of this finding, and structurally the same "duplicated logic, one gets the fix and others don't" risk pattern that caused the original deadlock bug) was **deliberately not done this pass**: a generic fan-out abstraction bridging three adapters with genuinely different pending-item types and per-item closures is a real refactor with real behavior-drift risk, judged higher-risk-than-value to bundle into the same pass as B.2's mechanical, easily-reviewed reordering — left as an explicit, tracked follow-up rather than silently dropped. Verified for real: re-ran the same real 7-page document against the same live `MinerU2.5-2604-1.2B` endpoint post-fix — 12.2s end-to-end (down from ~21s on the same endpoint pre-fix; consistent with, though not rigorously benchmarked proof of, more real request concurrency now that permits aren't wasted on CPU work), `page_errors: []`, `warnings: []`.

All changes in this batch: 214 unit + 23 CLI + 2 contract tests (default features), `clippy --all-targets` and `cargo fmt --check` both clean, plus the real-endpoint re-verification above. `CLI_ENHANCEMENT_PROPOSAL.md`'s B.1/B.4/B.5/B.2 entries updated to reflect completion; B.3 remains open.

**Post-P10 continued, third implementation batch — `--pages` range selection + long-run progress observability/stall watchdog (the two remaining P0 items from `CLI_ENHANCEMENT_PROPOSAL.md`'s original real-usage-driven list, both motivated directly by this session's own experience parsing the real 7-page/107-page documents).**

- **`--pages` (new `page_range.rs` module)**: `parse_page_range(&str) -> Result<Vec<u32>, String>` parses comma-separated single pages/`start-end` ranges (`"1-5"`, `"3"`, `"1,5,10-12"`), 1-indexed, sorted+deduplicated, with clean errors (not panics) for non-numeric tokens, inverted ranges, zero page numbers, and empty tokens. Wired into both `api.rs::ParseOptions.pages` and `cli.rs`'s new `--pages <RANGE>` flag — applied as a filter on the already-ingested `Vec<RenderedPage>`, right before the scheduler runs, so it works uniformly across every protocol/format without touching `ingest.rs` itself. A malformed `--pages` value is a clean `EXIT_USAGE` (not a panic), verified by `invalid_pages_value_is_a_usage_error`; `pages_filter_keeps_only_requested_pages` (CLI) and `parse_pages_filter_excludes_pages_not_in_the_requested_set`/`parse_pages_filter_keeps_matching_pages` (api.rs) prove the filter is genuinely applied, not silently ignored.
- **Progress reporting + stall watchdog (`scheduler.rs::run_with_progress` + `cli.rs`)**: `Scheduler::run` now delegates to a new `run_with_progress(..., on_page: impl FnMut(&PageProgress))` (backward compatible — `run`'s own signature/behavior is unchanged, `on_page` is a no-op for it), firing once per completed page with `{page_num, ok, completed, total}`. `cli.rs`'s non-streaming `parse` path (the `--stream` path already gets natural progress via its NDJSON output) wires this into two independent stderr mechanisms: (1) throttled progress lines (`progress: N/total pages (page P ok|error)`, at most one per ~900ms plus always the final page, skipped entirely for single-page documents where "no progress yet" is just normal startup latency) and (2) a background watchdog task checking every 5s whether *any* page has completed in the last 30s, and if not, printing a warning that includes the concurrency semaphore's current `available_permits()` count — this exact combination (elapsed-time-since-last-page + permit occupancy) is precisely the signal that was missing when this session spent significant time manually diagnosing the real scheduler deadlock via `ps`/`ss`/`nvidia-smi` instead of a single stderr line. **Verified for real on both real documents against the live `MinerU2.5-2604-1.2B` endpoint**, not just unit-tested: the 7-page document (which completes in ~12-21s) printed 2 progress lines (`1/7` then `7/7` — correctly throttled since all pages complete within the 900ms window of each other on a fast endpoint); the 107-page document printed 15 lines spread across the run (`1/107` ... `107/107`), a genuine live demonstration of the throttling working as designed rather than either flooding stderr with 107 lines or going silent. A dedicated scheduler-level test, `run_with_progress_invokes_callback_once_per_page_with_monotonic_completed_count`, proves the underlying mechanism (exactly one callback per page, `completed` values form a `1..=N` permutation under concurrent completion order) independent of CLI-layer throttling/watchdog logic.

All changes in this batch: 227 unit + 25 CLI + 2 contract tests (default features), 234 unit + 24 CLI + 2 contract (`--features native`), `clippy --all-targets` and `cargo fmt --check` both clean, plus the two real-endpoint verifications above. `CLI_ENHANCEMENT_PROPOSAL.md`'s "P0：`--pages`" and "P0：长任务可观测性" sections are both now implemented — every item from the original real-usage-driven P0 tier (postprocess/ingest_document/warnings wiring, B.1/B.4/B.5/B.2 concurrency fixes, `--pages`, progress/watchdog) is done except B.3 (the shared fan-out helper, deliberately deferred).

**Post-P10 continued, fourth implementation batch — D-tier VLM/OCR output correctness (D.1/D.3/D.4/D.13, the four items `CLI_ENHANCEMENT_PROPOSAL.md`'s deep-audit "D. VLM/OCR 输出内容与结构正确性" table flagged as highest-risk: real, silent content corruption rather than a crash or a loud error).**

- **D.1 — `geometry::rescale_bbox_to_original` no longer silently collapses every dots-ocr bbox to `[0,0,0,0]` on a degenerate page.** Two independent audit passes had separately flagged the same bug: when `original_wh` has a zero dimension, the scale factor divides to `inf`/`NaN`, and `as i32` then silently saturates that to `0` — every block on the page would report the exact same `[0,0,0,0]` bbox with no warning at all. The function's signature changed from `-> [i32; 4]` to `-> Option<[i32; 4]>`, returning `None` for a zero-dimension `original_wh`; `dots_ocr.rs` (the only caller) now skips that one cell and emits a `ctx.warn()` rather than corrupting the whole page's coordinates. Two new tests (`rescale_bbox_to_original_returns_none_for_zero_{width,height}_original`) prove the guard; the two pre-existing tests were updated to `.unwrap()` the now-`Option` return.
- **D.3 — the hand-written Python-literal string parser (`output_parse.rs::LiteralParser::parse_string`, MonkeyOCRv2's raw-output format) no longer corrupts real text on `\u`/`\x` escapes.** Previously any escape besides `\n`/`\t`/`\r` fell through to `other => other`, which silently dropped the backslash — for `中` (the Unicode escape for "中"), this produced the literal 5-character string `"u4e2d"` instead of decoding it, a real, silent data-corruption bug (not a parse failure) that a live model checkpoint emitting escaped non-ASCII text would hit routinely. Fixed with a new `LiteralParser::decode_hex_escape(marker, digits, out)` helper: `\uXXXX` (4 hex digits) and `\xHH` (2 hex digits) are now decoded into the real Unicode scalar; on any failure (too few digits, invalid hex, or a value that isn't a valid Unicode scalar, e.g. an unpaired surrogate half) it falls back to emitting the literal `\u`/`\x` marker without consuming the following characters, so they're re-parsed as plain text rather than silently misdecoded. Unrecognized escapes in general (not just `\u`/`\x`) now preserve their backslash (`other => { out.push('\\'); out.push(other); }`) instead of being eaten — recognized escapes that map to the same literal character (`\\`, `\'`, `\"`) are unaffected. Four new tests: `py_literal_decodes_unicode_escape_sequences` (the real "中文" case), `py_literal_decodes_hex_escape_sequences`, `py_literal_preserves_backslash_for_unknown_escapes`, `py_literal_falls_back_to_literal_marker_on_truncated_unicode_escape`.
- **D.4 — dots-ocr, the one protocol with zero bbox validation anywhere in its pipeline, now gets the same defensive clamp every other protocol already had.** The audit confirmed mineru-vlm's regex-based stage-1 parser rejects out-of-range coordinates earlier in its own pipeline and MonkeyOCRv2 already applies `map_bbox_0to1000_clamped`'s axis-swap+clamp+minimum-1px logic — dots-ocr alone had no equivalent, so a model that emitted an inverted or out-of-page box would flow straight through to `Block::bbox_px` unchecked. Added `geometry::sanitize_bbox_px(bbox, width, height) -> [i32; 4]`, factoring out `map_bbox_0to1000_clamped`'s already-proven clamp/swap/minimum-size logic into a form any adapter can call on an already-pixel-space bbox regardless of how it got there; wired into `dots_ocr.rs` right after `rescale_bbox_to_original`. Four new tests cover pass-through, axis-swap, out-of-page clamp, and the minimum-1px-size guarantee. mineru-vlm/MonkeyOCRv2's existing equivalent validation was deliberately left as-is rather than replaced with the shared helper, to avoid an unnecessary behavior-change risk on two adapters that weren't the audit's actual finding.
- **D.13 — the three chat-completions adapters (mineru-vlm/dots-ocr/MonkeyOCRv2) no longer treat a malformed backend response as "the model legitimately returned an empty string."** All three had a private `extract_content(resp) -> Option<&str>` that `.unwrap_or("")`'d a missing/null `choices[0].message.content` — a content-filter refusal, a malformed envelope, or any `finish_reason` other than `"stop"` would silently produce an empty string, which for the layout/single-round stage meant zero blocks for the whole page with no error at all (a blank-looking page, not a failure). Replaced the three private copies with one shared `adapters::extract_chat_content(resp: &Value) -> Result<&str, String>`, which distinguishes: an explicit `error` object in the response (surfaced verbatim), a present `choices[0]` with a non-`"stop"` `finish_reason` (surfaced with the reason), an explicitly-null `content` field, and a response missing `choices[0]` entirely (surfaced with the raw response body) — four distinct diagnostics instead of one silent empty string. Wired in at both call sites per adapter: the whole-page/stage-1 layout call now returns a `PageError` on failure (consistent with every other whole-page failure mode already in these adapters); the per-block stage-2 call in mineru-vlm/MonkeyOCRv2 now routes the error into that block's existing `Err(String)` per-block-isolation path (populating `Block.error`) rather than aborting the page, matching the existing block-level failure isolation those two adapters already had for network-dispatch errors. Five new tests directly on `extract_chat_content` cover the success case and all four failure diagnostics.

Verified for real against the same live `MinerU2.5-2604-1.2B` endpoint on the same real 7-page document used throughout this session (rebuilt with `--features pdfium` for real rasterization): `page_errors: []`, `warnings: []`, all 7 pages parsed correctly with the expected text/table/page-number content — confirming none of the four fixes altered behavior on a real, well-formed response; they only change what happens on a malformed/degenerate one. Full test suite: 242 unit + 25 CLI + 2 contract tests (default features), 249 unit + 24 CLI + 2 contract (`--features native`), `clippy --all-targets` and `cargo fmt --check` both clean. `CLI_ENHANCEMENT_PROPOSAL.md`'s D.1/D.3/D.4/D.13 table rows and its "实施顺序建议" item 4 are marked ✅ complete.

**Post-P10 continued, fifth implementation batch — D.2/D.5/D.8, the next tier of D-class VLM/OCR output-correctness findings (coordinate handling and category-vocabulary robustness, the same "silently produces wrong content" risk class as the fourth batch).**

- **D.2 — mineru-vlm's stage-1 `custom_token` parser (`output_parse.rs::parse_custom_tokens`) now clamps an out-of-range coordinate instead of discarding the whole box.** Previously any of the four `[0,1000]`-space coordinates exceeding 1000 caused the entire line to be dropped with a warning — but VLM quantization error nudging a coordinate 1-5 units past 1000 at a page edge is common and entirely recoverable, and MonkeyOCRv2's equivalent parser (`map_bbox_0to1000_clamped`) already clamps rather than discards for exactly this reason. Changed to `[x1.min(1000), y1.min(1000), x2.min(1000), y2.min(1000)]`, only emitting a warning when clamping actually changed a value (so well-formed lines stay silent, avoiding warning-fatigue on the common case). Updated `out_of_range_coordinate_is_clamped_with_warning_not_dropped` (renamed from `..._is_skipped_with_warning`, now asserting the box survives with clamped coordinates) and added `well_formed_coordinates_produce_no_clamp_warning`.
- **D.5 — `imaging::crop` no longer fabricates a bogus 1x1 image for a bbox that doesn't overlap the page at all.** Previously `(x1 - x0).max(1)`/`(y1 - y0).max(1)` silently turned a fully-out-of-bounds or degenerate bbox (e.g. from an upstream coordinate-system bug) into a fake 1x1 crop, which was then sent to the model as if it were real content — no signal anywhere that the crop region was bogus. Changed `crop`'s signature from `-> RgbImage` to `-> Option<RgbImage>`, returning `None` when the clamped region has zero width or height. `ParseCtx::crop` (the one caller, in `adapters/mod.rs`) converts `None` into `Err(format!("crop region {bbox_px:?} does not overlap the page at all"))` — no adapter-side changes were needed at all, since all three existing callers (`mineru_vlm.rs`/`monkeyocr_v2.rs`/`pipeline.rs`) already pattern-match `ctx.crop()`'s `Result` and route an `Err` into that block's existing per-block error-isolation path. Two new tests: `crop_returns_none_when_bbox_is_entirely_off_page`, `crop_returns_none_for_a_degenerate_zero_size_bbox`.
- **D.8 — all four `category_map.rs` mapper functions now share one normalization step before matching, instead of only `map_mineru_vlm_category` being effectively defended.** The audit's finding: `map_mineru_vlm_category` only appeared normalized because its caller (`parse_custom_tokens`) already lowercases the raw category upstream — the mapper itself did an exact-string match, same as `map_dots_ocr_category`/`map_monkeyocrv2_category`/`map_pipeline_category`, none of which had any defense against a model emitting a slightly different case or separator (e.g. `"Text"` instead of `"text"`, `"list_item"` instead of `"List-item"`) for a category. The real `list_item` vocabulary drift this project already hit on mineru-vlm (see the category_map.rs module doc) was only caught because it happened to land in the one place with any normalization logic upstream — the other three protocols had no equivalent safety net at all. Added a shared `normalize_key(raw: &str) -> String` (lowercase, strip `-`/`_`/space) and switched all four `map_*_category` functions to match on `normalize_key(raw).as_str()` instead of `raw` directly, updating each match arm's keys to their normalized form (e.g. `"List-item"` → `"listitem"`, `"page_number"` → `"pagenumber"`). Six new tests directly exercise the normalization robustness across three of the four mappers (`dots_ocr_category_matching_is_case_and_hyphen_insensitive`, `monkeyocrv2_category_matching_is_case_and_hyphen_insensitive`, `pipeline_category_matching_is_case_and_hyphen_insensitive`), plus all pre-existing per-mapper tests continued to pass unmodified (they already used the documented native spelling, which normalizes to itself).

Verified for real against the same live `MinerU2.5-2604-1.2B` endpoint on the same real 7-page document (rebuilt with `--features pdfium`): `page_errors: []`, `warnings: []`, all 7 pages parsed correctly — confirming none of these three fixes altered behavior on a real, well-formed response; they only change what happens on a degenerate/drifted one. Full test suite: 248 unit + 25 CLI + 2 contract tests (default features), 255 unit + 24 CLI + 2 contract (`--features native`), `clippy --all-targets` and `cargo fmt --check` both clean. `CLI_ENHANCEMENT_PROPOSAL.md`'s D.2/D.5/D.8 table rows and its "实施顺序建议" item 4b are marked ✅ complete.

**Post-P10 continued, sixth implementation batch — D.6/D.7, the table-structure-parsing half of the D-tier findings (both in `otsl.rs`, the same file, both about `otsl::to_html` silently trusting something it hadn't verified).**

- **D.6 — `otsl.rs`'s `xcel` token (extends a cell both up and left simultaneously) no longer silently prefers "up" when its up-neighbor and left-neighbor are two genuinely distinct cells that disagree.** The module's own doc comment already flagged this as "a best-effort implementation... most likely to break on real irregular table headers," and the audit confirmed the code backed that up: `xcel`'s referent lookup was `referent_up.or_else(|| referent_left)`, so whenever both existed, `up` silently won with zero consistency check. Rewrote the branch to compute both `referent_up`/`referent_left` explicitly; when both exist and are different cells, it picks whichever already covers the larger `(rows × cols)` span — a reasonable tie-break, since the bigger existing merge is more likely the real origin cell — and now pushes a warning describing the conflict and which side won, instead of deciding silently. `to_html`'s signature changed from `-> String` to `-> (String, Vec<String>)` to carry these (and D.7's) warnings out; the four call sites (`mineru_vlm.rs`, `monkeyocr_v2.rs`, `dots_ocr.rs`, `pipeline.rs::recognize_table`) were all updated to route the returned warnings through `ctx.warn()`, the same pattern every other adapter-level recovery warning already uses — `pipeline.rs::recognize_table` needed a small additional change (threading `page_num` through as a new parameter) since it didn't previously have page context available at its warn call site. New test `xcel_span_conflict_between_distinct_up_and_left_cells_is_resolved_and_warned` constructs a genuine three-independent-cells-then-merge scenario (not the already-fully-merged 2x2 case the pre-existing `two_d_span_via_xcel` test covers, which correctly produces zero warnings since up and left resolve to the *same* cell there) and asserts both the conflict warning and a successfully-rendered table.
- **D.7 — `otsl::to_html`'s literal-HTML passthrough now checks for a real closing `</table>` tag, not just the opening one.** Previously `trimmed.to_lowercase().starts_with("<table")` alone was enough to trust the entire string as complete HTML and return it verbatim — a truncated model response that opens a table but never closes it (a realistic failure mode for any token-budget-limited or interrupted generation) would have been passed straight through as "complete" HTML with a dangling unclosed tag. Now requires `starts_with("<table")` *and* `ends_with("</table>")` before trusting the passthrough; if only the former matches, it falls through to OTSL tokenization instead (which, for genuine truncated HTML, will find no OTSL tags either and degrade to the existing `"<table></table>"` empty-table fallback) and pushes a warning rather than silently emitting truncated markup. Two new tests: `unclosed_table_like_prefix_is_not_trusted_as_passthrough` (asserts the output differs from the truncated input and a warning is present) and `properly_closed_table_passthrough_is_still_trusted` (confirms the fix doesn't regress the legitimate case, case-insensitively).

All pre-existing `otsl.rs` tests were updated to destructure the new `(String, Vec<String>)` return (mechanical, not behavioral, changes) plus asserting `warnings.is_empty()` on the known-clean cases. Verified for real against the same live `MinerU2.5-2604-1.2B` endpoint on the same real 7-page document (rebuilt with `--features pdfium`): `page_errors: []`, `warnings: []`, all 7 pages parsed correctly including its table block — confirming neither fix altered behavior on real, well-formed table output; they only change what happens on a conflicting/truncated one. Full test suite: 251 unit + 25 CLI + 2 contract tests (default features), 258 unit + 24 CLI + 2 contract (`--features native`), `clippy --all-targets` and `cargo fmt --check` both clean. `CLI_ENHANCEMENT_PROPOSAL.md`'s D.6/D.7 table rows and its "实施顺序建议" item 4c are marked ✅ complete.

**Post-P10 continued, seventh implementation batch — D.9/D.10, the formula/LaTeX-handling half of the D-tier findings.**

- **D.9 — `formula_repair::balance_brackets` now handles the "more closers than openers" direction it previously ignored entirely, but deliberately does *not* attempt the "fix" the audit originally proposed.** Re-checked the audit's example, `\left[ \frac{a}{b} \right)`, against real LaTeX semantics: `\left`/`\right` don't require matching bracket glyphs — `\left[a,b\right)` is the standard, valid way to typeset a half-open interval. Rewriting it to force matching delimiter types would silently corrupt a semantically correct formula, so that specific "fix" was rejected after verification, not implemented blindly. What *is* a genuine, verified gap: the function only ever handled `opens > closes` (appending missing `}`) and `left_count > right_count` (appending missing `\right.`) — a response with the reverse imbalance (an extra `}` with no matching `{`, or a stray `\right` with no `\left`, both plausible from a truncated/malformed model response) was silently left untouched. Both directions are now handled symmetrically via `match ... .cmp(...)`, prepending `{`/`\left.` for the reverse case. Four new tests: `balance_brackets_leaves_mismatched_but_balanced_delimiter_types_untouched` (proves the half-open-interval case is correctly left alone), `balance_brackets_prepends_missing_opening_brace`, `balance_brackets_prepends_missing_left_for_orphan_right`.
- **D.10 — dots-ocr's formula output is now wrapped in LaTeX display-math delimiters like mineru-vlm's, instead of emitting bare unwrapped LaTeX that silently fails to render as math.** Confirmed the actual downstream impact first: `render.rs`'s Markdown renderer emits `Block.latex` verbatim into the output — an unwrapped formula (`\frac{1}{2}`, no `\[...\]`/`$$...$$`) prints as plain inline text in the final Markdown rather than being recognized as display math by any standard Markdown+KaTeX renderer, a real, confirmable content defect, not just a stylistic inconsistency. Extracted mineru-vlm's existing `wrap_display_math` (idempotent — leaves already-`\[`/`$$`-wrapped content untouched) out of `mineru_vlm.rs` into a new shared `formula_repair::wrap_display_math`, and wired dots-ocr's `Formula` branch to call it too. **Deliberately left MonkeyOCRv2's own `$$...$$` wrapper as-is rather than unifying all three to one style**: checked the real vendored source (`opensource/MonkeyOCRv2/parsing/core_runner.py:1040,2136`) and confirmed `$$...$$` is MonkeyOCRv2's actual real-protocol convention, not an arbitrary choice — forcing it to `\[...\]` would have reduced fidelity to the real upstream for no correctness benefit. Found one closely-related, genuine fidelity gap while doing this check, though: the real source's wrap step has its own idempotency guard (`not content.lstrip().startswith("$$")`) that this project's port of it was missing, so a formula the model had already wrapped itself would get double-wrapped into `$$\n$$\n...\n$$\n$$` — fixed by adding the equivalent check to `monkeyocr_v2.rs`'s local `wrap_display_math`. Updated `dots_ocr.rs`'s existing formula test to expect the now-wrapped output; added `wrap_display_math_*` tests in both `formula_repair.rs` (bare-wrap, idempotent-for-`\[`, idempotent-for-`$$`) and `monkeyocr_v2.rs` (bare-wrap, idempotent-for-`$$`).

Verified for real against the same live `MinerU2.5-2604-1.2B` endpoint on the same real 7-page document (rebuilt with `--features pdfium`): `page_errors: []`, `warnings: []`, all 7 pages parsed correctly — confirming neither fix altered behavior on real, well-formed formula output. Full test suite: 259 unit + 25 CLI + 2 contract tests (default features), 266 unit + 24 CLI + 2 contract (`--features native`), `clippy --all-targets` and `cargo fmt --check` both clean. `CLI_ENHANCEMENT_PROPOSAL.md`'s D.9/D.10 table rows and its "实施顺序建议" item 4d are marked ✅ complete. Remaining open items: B.3, D.11/D.12 (the last two D-tier items), C-tier network retry robustness (429/backoff/dispatch_rest timeout config), `content_normalize.rs`, `robustness.rs` wiring, config-file support, Markdown rendering enhancements, F-tier CLI consistency fixes, G-tier SSRF hardening, H-tier IR/schema clarity.
