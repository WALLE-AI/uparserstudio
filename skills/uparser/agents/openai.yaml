interface:
  display_name: "uparser Document Parser"
  short_description: "Plan, route, and parse mixed-format documents"
  default_prompt: "Use $uparser to plan and parse this document with the appropriate Architecture V2 path, preserving routing evidence and fidelity warnings."
